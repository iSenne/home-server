apiVersion: apps/v1
kind: Deployment
metadata:
  name: isenne-dev
spec:
  selector:
    matchLabels:
      app: isenne-dev
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: isenne-dev
    spec:
      containers:
        - name: isenne-dev
          image: bitnami/wordpress
          ports:
            - containerPort: 8080
          env:
            - name: WORDPRESS_ENABLE_REVERSE_PROXY
              value: "yes"
            - name: WORDPRESS_AUTO_UPDATE_LEVEL
              value: "minor"
            - name: WORDPRESS_ENABLE_HTTPS
              value: "yes"
            - name: WORDPRESS_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: isenne-dev-secret
                  key: WORDPRESS_DB_PASSWORD
            - name: WORDPRESS_DATABASE_HOST
              valueFrom:
                configMapKeyRef:
                  name: isenne-dev-config
                  key: WORDPRESS_DB_HOST
            - name: WORDPRESS_DATABASE_USER
              valueFrom:
                configMapKeyRef:
                  name: isenne-dev-config
                  key: WORDPRESS_DB_USER
            - name: WORDPRESS_DATABASE_NAME
              valueFrom:
                configMapKeyRef:
                  name: isenne-dev-config
                  key: WORDPRESS_DB_NAME
          volumeMounts:
            - name: isenne-dev-storage
              mountPath: /bitnami/wordpress/isenne_dev
          resources:
            requests:
              memory: "256Mi"
              cpu: "125m"
            limits:
              memory: "512Mi"
              cpu: "250m"
      volumes:
        - name: isenne-dev-storage
          persistentVolumeClaim:
            claimName: isenne-dev-lh-pvc
